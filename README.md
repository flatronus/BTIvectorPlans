# BTIvectorPlans
Редактор планів БТІ

---

## Журнал версій

| Дата і час | Версія | Опис змін |
|---|---|---|
| 2026-05-18 18:00 | v0.1.0 | Початковий стан проєкту: `index.html`, `script.js`, `app.css`, `constants.js`, `state.js`, `toast.js`. Базовий редактор фігур із підтримкою полілінії, замикання, free-ліній, ієрархії елементів, мультиканви з вкладками, збереження SVG, toast-повідомлень. |
| 2026-05-19 12:00 | v0.2.0 | Рефакторинг: `script.js` розбито на 9 модулів без import/export (підключення через `<script>` в `index.html`). Додано `g.js` — глобальний змінний стан `window.G`. Нові файли: `svg-primitives.js`, `elements-on-line.js`, `canvas-manager.js`, `hierarchy.js`, `shape-editor.js`, `shape-transfer.js`, `coord-modal.js`, `lines-panel.js`, `quick-shape.js`, `main.js`. Файл `script.js` виведено з використання. |
| 2026-05-19 20:00 | v0.2.1 | Панель інструментів: прибрано текстову назву програми (залишено лише іконку логотипу); прибрано кнопку «Публікувати»; виправлено дьоргання кнопок при наведенні — тултіпи перенесено в атрибут `title`, кастомні `.tooltip`-div видалено; кнопки тепер переносяться на малих екранах (`flex-wrap`); на мобільних пристроях написи кнопок приховано через клас `.toolbar-label` (медіа-запит `md:`). Кнопка «Зберегти» на мобільному тепер завантажує `.svg`-файл напряму (через `<a download>`), а не показує модалку копіювання. |
| 2026-05-19 21:00 | v0.2.2 | Канва: прибрано `p-4` (padding=0). Панель ієрархії: кнопка відкриття/приховування в тулбарі (`toggleHierarchyPanel`); на мобільних — оверлей поверх канви, ширина max 2/3 екрану; вузька прокрутка (4px, `scrollbar-width: thin`). Редактор фігур: адаптивна панель кнопок (`flex-wrap`, `.shape-modal-toolbar`); написи кнопок ховаються на мобільних (`.shape-btn-label`); заголовок «Редактор фігур» ховається на мобільних. Додано кнопку «Відміна» (`cancelShapeModal`) — закриває редактор без збереження. |
| 2026-05-20 14:00 | v0.2.3 | Виправлено баг: при очищенні поля вводу в модалці координат лінія більше не зникає — порожнє поле тепер просто закриває модалку без змін; захист від нульової довжини в `updateExistingLine`. Реалізовано кнопку «Діагональ»: відкриває модалку з полем вводу формату `1 3 4,52` (точка1 точка2 відстань); переміщує проміжну точку так, щоб задовольнити задану діагональ (підтримується один проміжний вузол); нові функції `_applyDiagonalConstraint`, `redrawEntireFigureFromPoints` у `shape-editor.js`; нові функції `closeDiagonalModal`, `applyDiagonal` у `coord-modal.js`; нова модалка `#diagonalModal` в `index.html`. |
| 2026-05-20 16:00 | v0.2.4 | Виправлено логіку діагоналі та ланцюгову систему координат. Ланцюг: точка 1 фіксована, кожна наступна будується від кінця попередньої лінії; при редагуванні лінії N — всі наступні точки перераховуються автоматично, замикаюча лінія підлаштовує свою довжину. Діагональ тепер: фіксує pt1 і всі лінії до неї, рухає тільки pt2 (кінець лінії pt1→pt2) через теорему косинусів; лінія pt1→pt2 стає free з _cachedEnd; відображається штриховою лінією з розміром у редакторі; на головному полотні невидима; не бере участі в розрахунку площі; видима в списку ліній з позначкою «╌╌ діаг.», редагується. Нові функції: `_rebuildChainPoints`, `_updateClosingLineLength` у `shape-editor.js`; `_renderSvgDashedLine` у `svg-primitives.js`. |
| 2026-05-20 17:30 | v0.2.5 | Виправлено: кнопка Ок у вікні «Введіть координати» тепер гарантовано закриває модалку — `closeCoordModal` обгорнуто в `try/finally`, тому `display=none` виконується навіть при JS-помилці всередині (наприклад, при відсутності нової функції `_renderSvgDashedLine` через кеш браузера). Додано fallback у `redrawEntireFigure`: якщо `_renderSvgDashedLine` недоступна — використовується звичайна лінія. |
| 2026-05-20 18:00 | v0.2.6 | Виправлено критичну синтаксичну помилку в `shape-editor.js`: мультирядковий рядок у `diagLineData.code` (literal newlines замість `\n`) ламав весь файл при завантаженні — через це `closeCoordModal` отримував "Помилка обробки координат" на будь-якому вводі. Замінено на коректний рядок з екранованими `\n`. |
| 2026-05-20 18:30 | v0.2.7 | Виправлено `_applyDiagonalConstraint`: тепер шукає лінію що ЗАКІНЧУЄТЬСЯ в pt2Num (лінія X→pt2), а не пряму лінію pt1→pt2 — саме так рухається правильна точка; логіка перетину двох кіл (коло C1(pFrom, lineLen) і C2(p1, diagPx)) замінила теорему косинусів; на початку викликається `_rebuildChainPoints()` для синхронізації G.shapePoints; виправлено `_rebuildChainPoints` — коректна обробка free-ліній без `_cachedEnd`. |
| 2026-05-20 19:00 | v0.2.8 | Відносні напрямки ліній: кнопки top/right/bottom/left у вікні «Введіть координати» тепер відносні до попередньої лінії (top=вперед, right=поворот праворуч 90°, bottom=розворот 180°, left=поворот ліворуч 90°). Нові хелпери `_getPrevLineVector` і `_calcRelativeEnd` у `shape-editor.js`. `_rebuildChainPoints` оновлено — відносний вектор передається по ланцюгу між лініями. `quick-shape.js` оновлено — прямокутник і трикутник зберігають `_cachedEnd` з явними координатами і direction='free', щоб не конфліктувати з відносною системою. |
| 2026-05-20 20:00 | v0.2.9 | Порожнє поле + OK при редагуванні → видаляє лінію і всі наступні (`deleteLineAndFollowing`). Перша лінія завжди горизонтальна (дефолтний вектор (1,0)). Кнопки напрямку спрощено: прибрано top/bottom/В, залишено тільки right (поворот за год. −90°) і left (проти год. +90°). Побудова ліній за годинниковою стрілкою. Редагування довжини діагоналі: при натисканні на діагональ у списку ліній відкривається модалка діагоналі з поточними значеннями. Оновлено `parseCoordinateInput` — валідні напрямки тепер тільки right/left/free/diagonal. |
| 2026-05-20 20:30 | v0.2.10 | Перша лінія завжди горизонтальна: кнопки right/left визначають тільки напрямок від точки 1 (right = вправо, left = вліво). Для всіх наступних ліній right/left залишаються відносними поворотами від попередньої. Прапорець `isFirstLine` в `_rebuildChainPoints` і перевірка `hasLines` в `_calcRelativeEnd` розрізняють два режими. |
| 2026-05-20 20:45 | v0.2.11 | Виправлено формули повороту right/left: right = перпендикуляр праворуч від напрямку руху (-uy, ux), left = перпендикуляр ліворуч (uy, -ux). Приклад: попередня лінія вліво (-1,0) + кнопка left → вниз (0,1). |
| 2026-05-20 21:00 | v0.2.12 | Виправлено видалення ліній: `deleteLineAndFollowing` тепер видаляє тільки лінії що починаються/закінчуються на точках що зникають — діагоналі між точками що залишаються зберігаються. Додано кнопку «Відмінити» у вікні «Введіть координати» (`cancelCoordModal`) — закриває без жодних змін. Escape і клік на тло також викликають «Відмінити». |
| 2026-05-21 14:30 | v0.2.13 | Ієрархія елементів: елементи (WI1 та ін.) у дереві ієрархії стали клікабельними — підсвічуються при наведенні. При кліку на елемент відкривається редактор фігур у режимі перегляду: відображається лінія-хост (та лінія фігури, на якій розміщено елемент) з точками, розміром і самим елементом. Бокова панель показує інформацію про елемент: приміщення, номер лінії, позицію і довжину. Кнопки OK і Відміна закривають перегляд без змін на полотні. Нові поля в `state.js`: `viewingElementMode`, `viewingElementSource`, `viewingElementTransform`. Змінено: `hierarchy.js`, `shape-editor.js`, `lines-panel.js`, `state.js`, `main.js`. |
| 2026-05-21 15:30 | v0.2.14 | Виправлено 4 баги. 1) Вікно «Діагональ між точками» тепер гарантовано закривається після «Застосувати» — `applyDiagonal` обгорнуто в `try/finally`. 2) Права панель «Лінії фігури» більше не порожня при відкритті редактора для існуючої фігури — `openShapeModalForEdit` скидає `viewingElementMode=false`. 3) Фігура не виходить за межі тла при повторному відкритті редактора — `resetSvgCanvas` скидає `viewBox` до `0 0 800 600`; `autoScaleAndCenterFigure` фільтрує `isTemp`-точки. 4) Елемент вікна у режимі перегляду відображається в оригінальній орієнтації та позиції як на головному полотні. Змінено: `coord-modal.js`, `hierarchy.js`, `shape-editor.js`, `svg-primitives.js`. |
| 2026-05-21 16:30 | v0.2.15 | Виправлено 2 баги. 1) Панель «Лінії фігури» була повністю порожня — синтаксична помилка в `lines-panel.js`: після блоку `return` для режиму перегляду елемента пропав рядок `linesList.appendChild(_makeCheckboxRow(` (наслідок невдалого str_replace в попередній версії); весь код нижче не виконувався. 2) Фігура в редакторі фігур не по центру тла — `autoScaleAndCenterFigure` формувала viewBox без врахування співвідношення сторін SVG; при горизонтальній або вертикальній лінії висота/ширина viewBox була мінімальною, а `xMidYMid meet` зсував фігуру до краю. Тепер viewBox завжди квадратний (розмір = max(ширина, висота) фігури + відступи), центр viewBox = центр фігури. Змінено: `lines-panel.js`, `shape-editor.js`. |
| 2026-05-21 20:00 | v0.2.16 | Режим редагування елемента (вікна WI1). При кліку на вікно в ієрархії відкривається редактор у режимі РЕДАГУВАННЯ (не перегляду): показується лінія-хост з вікном; два кути вікна на протилежному боці позначаються A і B (фіолетові). Кнопка «Додати» в тулбарі відкриває вікно координат для малювання нових ліній від точки 1, дотичних до вікна. У вікні координат показується блок прив'язки: відстань від т.1 до кутів A і B; кнопки A/B вставляють цю відстань у поле вводу. У списку «Лінії фігури» вікно відображається окремим рядком — клік відкриває модалку зміни товщини (Enter або OK застосовує, Відміна скасовує). Кнопки «Замкнути», «Діагональ», «Швидко» ховаються в режимі елемента. Нові поля: `appState.editingElementThickness`, `appState._addingElementLine`; нові масиви в G: `elementEditorLines`, `elementEditorPoints`, `elementEditorCounter`. Нові функції: `_redrawElementEditorCanvas`, `_renderWindowCornerLabel`, `_addLineToElementEditor`, `addElementEditorLine`, `_updateWindowCornerAnchorUI`, `insertWindowCornerAnchor`, `shapeModalAddAction`, `_updateShapeModalToolbar`, `openElementThicknessModal`, `applyElementThickness`. Змінено: `shape-editor.js`, `lines-panel.js`, `coord-modal.js`, `state.js`, `g.js`, `main.js`, `index.html`, `hierarchy.js`. |
| 2026-05-21 21:00 | v0.2.17 | Виправлено 2 проблеми. 1) Малюнок скакав і ставав дуже малим після зміни товщини — `_redrawElementEditorCanvas` включав START_X/START_Y (400,300) до масиву allX/allY при розрахунку viewBox, що робило viewBox набагато більшим за реальний вміст; тепер viewBox будується тільки навколо хост-лінії, кутів A/B і доданих точок. Точка 1 розміщується поруч із кутом A (не в глобальних константах). 2) Інфо про елемент і кнопка «Товщина» перенесені з панелі «Лінії фігури» у верхній тулбар Редактора фігур (з'являються тільки в режимі елемента): спан з кодом елемента, лінією, позицією і поточною товщиною + кнопка «Товщина» для зміни. Після застосування нової товщини тулбар оновлює текст «т=…». Змінено: `shape-editor.js`, `main.js`, `index.html`. |
| 2026-05-22 01:00 | v0.2.21 | Прибрано інформаційні написи (хост-лінія, елемент, підказка A/B) з панелі Лінії фігури в режимі елемента. Кнопка Товщина повернута на верхню панель Редактора фігур. Кнопки Діагональ/Швидко/Замкнути більше не ховаються. Прибрано ElInfo span з index.html. Змінено: `lines-panel.js`, `main.js`, `index.html`. |
| 2026-05-23 12:00 | v0.2.22 | Поле «Товщина вікна» змінено з `type="number"` на `type="text"`, прибрано атрибути `min`/`max`/`step` — браузер більше не блокує кому. В `applyElementThickness` додано `.replace(',', '.')` перед `parseFloat`. Змінено: `index.html`, `main.js`. |
| 2026-05-23 12:30 | v0.2.23 | У вікно «Товщина вікна» додано поле «Прив'язка (м)» (`elementBindingInput`). Значення зберігається в `appState.editingElementBinding`. Обидва поля приймають кому як роздільник. Toast після OK показує обидва значення. Змінено: `index.html`, `state.js`, `main.js`. |
| 2026-05-23 13:00 | v0.2.24 | Мітки кутів вікна перейменовано з A/B на 1/2: SVG-мітки в редакторі елемента, підказка відстані («до A»→«до 1»), кнопки прив'язки у coord modal, логіка вибору кута (`corner === 'A'` → `corner === '1'`). Змінено: `shape-editor.js`, `coord-modal.js`, `index.html`. |
| 2026-05-23 14:30 | v0.2.25 | Поле «Прив'язка» у вікні «Товщина вікна» змінено на двокомпонентний формат: `1 2.00` — де `1` або `2` означає номер кута вікна, а `2.00` — відстань у метрах від цього кута вздовж лінії вікна. В `applyElementThickness` реалізовано парсинг двох чисел; значення зберігається як об'єкт `{ corner, dist }` в `appState.editingElementBinding`. У `_redrawElementEditorCanvas` додано малювання лінії прив'язки: від обраного кута вікна (1→wA, 2→wB) у напрямку до протилежного кута на задану відстань; лінія і розмір відображаються в редакторі; нова точка позначається помаранчевим кружком. `appState._bindingPoint` зберігає координати нової точки для включення в розрахунок viewBox. Змінено: `index.html`, `main.js`, `shape-editor.js`, `state.js`. |
| 2026-05-23 15:00 | v0.2.26 | Виправлено напрямок лінії прив'язки: нова точка тепер відкладається в напрямку ГЕТЬ від протилежного кута, тобто «назовні» від обраного кута. corner=1 (anchor=wA): напрямок wA−wB; corner=2 (anchor=wB): напрямок wB−wA. Змінено: `shape-editor.js`. |
| 2026-05-23 15:20 | v0.2.27 | Виправлено парсинг відстані прив'язки: регекс розбивки `[\s,;]+` розбивав число `2,36` на `2` і `36` (кома є роздільником десятків). Замінено на `[\s;]+` — тепер кома в числі не є роздільником між двома параметрами; `.replace(',', '.')` замінює кому на крапку вже в числі. Змінено: `main.js`. |
| 2026-05-23 16:00 | v0.2.28 | Збереження результату редагування елемента на головне полотно. Кнопка «OK» в редакторі фігур (режим елемента) тепер: (1) оновлює товщину WI1 на головному полотні — перемальовує SVG-групу батьківської фігури з `_elementThickness` збереженою в `lineData`; (2) малює лінію прив'язки у SVG-групі батьківської фігури з розмірним підписом; (3) додає в ієрархію «Кімнату» дочірньою до батьківської фігури (де знаходиться вікно). Товщина і прив'язка зберігаються в `el.thickness` / `el.binding` і відновлюються при повторному відкритті елемента. Змінено: `main.js`, `shape-editor.js`, `shape-transfer.js`. |
| 2026-05-23 16:30 | v0.2.29 | Виправлено: товщина і прив'язка вікна не відновлювались при повторному відкритті елемента з ієрархії. Причина: `el` в `openElementInShapeEditor` — новостворений об'єкт з `extractLineElements`, а не той де зберігались `el.thickness`/`el.binding`. Виправлення: товщина і прив'язка зберігаються прямо в `lineData` батьківської фігури (`_elementThickness`, `_elementBinding`); `openElementInShapeEditor` читає їх з `hostLine._elementThickness` / `hostLine._elementBinding`. Змінено: `shape-editor.js`, `shape-transfer.js`. |
| 2026-05-23 17:00 | v0.2.30 | При кліку на «Кімната (прив'язка)» в ієрархії відкривається звичайний редактор фігур з першою лінією вже накресленою: точка 1 = точка прив'язки, точка 2 = кут вікна. Лінія редагована як звичайна — можна додавати наступні лінії, отвори, змінювати довжину. Дочірня кімната тепер має `figureLines`, `shapePoints` і власну `svgGroup` на головному полотні з першою лінією і розмірним підписом. При повторному натисканні OK — оновлює існуючу кімнату. Змінено: `shape-transfer.js`. |
| 2026-05-23 17:30 | v0.2.31 | Виправлено прив'язку дочірньої кімнати до вікна на головному полотні. При натисканні OK у редакторі кімнати фігура тепер лягає точно на точку прив'язки: зберігається `_anchorOnCanvas: {x, y}` — абсолютна координата точки прив'язки на SVG. У `transferFigureToMainCanvas` при редагуванні елемента з `_anchorOnCanvas` `offsetX/Y` обчислюється як `anchor - START_X/Y` замість центрування по A4. Змінено: `shape-transfer.js`. |
| 2026-05-23 18:00 | v0.2.32 | Виправлено редагування першої лінії дочірньої кімнати: при зміні довжини `free`-лінії через `editLine` → `updateExistingLine` тепер перераховується `_cachedEnd` зі збереженням напрямку (нормований вектор старого кінця × нова довжина × SCALE). Раніше `_cachedEnd` залишався зі старими координатами і `_rebuildChainPoints` використовував стару позицію. Змінено: `shape-editor.js`. |
| 2026-05-24 10:00 | v0.2.33 | Виправлено: вікно (WI1) що додавалось на лінію 2-3 з'являлось на лінії 1-2. Причина: `openShapeModalForEdit` не відновлював `G.lineIdCounter` і `G.pointCounter` — нова лінія 2-3 отримувала `id=1`, конфліктуючи з існуючою лінією 1-2. Виправлення: в `openShapeModalForEdit` встановлюємо `G.lineIdCounter = max(id) + 1` і `G.pointCounter = max(num)` з завантажених `figureLines` і `shapePoints`. Змінено: `hierarchy.js`. |
| 2026-05-24 11:00 | v0.2.34 | Виправлено зсув вкладених кімнат при повторному редагуванні. Проблема: `_anchorOnCanvas` зберігався як абсолютна SVG-координата і не перераховувався коли батьківська фігура переміщувалась. Рішення: (1) введено `_anchorDef` — параметричний опис прив'язки `{hostLineId, elStart, elEnd, elSide, thickness, corner, dist}`; (2) нова функція `_computeAnchorOnCanvas` перераховує абсолютний якір з `_anchorDef` і поточного offsetX/Y батька; (3) нова функція `_refreshChildAnchors` рекурсивно оновлює `_anchorOnCanvas` і перемальовує SVG-групи всіх дочірніх кімнат після зміни батька; (4) `_refreshChildAnchors` викликається після `_rebuildSvgGroup` в `transferFigureToMainCanvas` і `transferElementResultToMainCanvas`. Скільки б вкладень не було — кожне перераховується від параметрів, не від старих абсолютних координат. Змінено: `shape-transfer.js`. |
| 2026-05-24 11:30 | v0.2.35 | Виправлено синтаксичну помилку в `shape-transfer.js`: при додаванні `_refreshChildAnchors` залишився уривок коментаря `* 1. ...` без відкриваючого `/**` між функціями — це ламало весь JS файл і блокувало кнопку OK в редакторі фігур. Відновлено `/**` перед коментарем до `transferElementResultToMainCanvas`. Змінено: `shape-transfer.js`. |
| 2026-05-24 12:30 | v0.2.36 | Виправлено зсув прив'язаних фігур. Корінна причина: лінія прив'язки малювалась у батьківській SVG-групі (`item.svgGroup.appendChild(bLine)`) — але `_rebuildSvgGroup` батька при кожному OK очищає і перемальовує групу через `_fillSvgGroup`, яка про лінію прив'язки нічого не знає. В результаті лінія зникала, а `child.svgGroup` при оновленні очищалась але не перемальовувалась. Виправлення: лінія прив'язки малюється ТІЛЬКИ в `child.svgGroup` через `_fillSvgGroup(childGroup, childOffsetX, childOffsetY)` — і при створенні, і при оновленні (`_rebuildSvgGroup`). `childOffsetX/Y = bx/by - START_X/Y`. При `_refreshChildAnchors` теж використовується `_rebuildSvgGroup` → `_fillSvgGroup` — тому перемальовка завжди консистентна. Змінено: `shape-transfer.js`. |
| 2026-05-24 13:00 | v0.2.37 | Виправлено зсув фігур на головному полотні при повторному редагуванні. Причина: при кожному OK `transferFigureToMainCanvas` перераховував `offsetX/Y` як центр фігури по A4 — але після додавання нових ліній центр змінювався і фігура зміщувалась. Виправлення: при першому розміщенні фігури зберігається `item._offsetX/_offsetY`; при повторному редагуванні використовується цей збережений offset (для прив'язаних фігур — `_anchorOnCanvas`, для звичайних — `_offsetX/_offsetY`). Змінено: `shape-transfer.js`. |
| 2026-05-24 14:00 | v0.2.38 | Виправлено зсув фігури після редагування елемента (вікна). Причина: `transferElementResultToMainCanvas` при перемалюванні SVG-групи батьківської фігури щоразу перераховував `offsetX/Y` від центру A4, ігноруючи збережений `_offsetX/_offsetY`/`_anchorOnCanvas`. Якщо вікно розташоване не в центрі A4, після натискання OK фігура стрибала на нову позицію. Виправлення: замість перерахунку від центру A4 — беремо `item._anchorOnCanvas` (якщо є), інакше `item._offsetX/_offsetY`; fallback на центр A4 залишено тільки для дуже старих елементів без збереженого offset. Змінено: `shape-transfer.js`. |
| 2026-05-24 15:00 | v0.2.39 | Виправлено зникнення глибоко вкладених фігур при зміні товщини вікна. Причина 1: після перемалювання існуючої дочірньої кімнати (`existingChild`) функція робила `return` не відновивши `G.figureLines`/`G.shapePoints` до стану батьківської фігури — всі подальші рівні ієрархії малювались з дитячими координатами і зникали. Причина 2: `_refreshChildAnchors` рахував якір від старої товщини вікна (`anchorDef.thickness`), не беручи до уваги щойно змінену `hostLineData._elementThickness`. Виправлення: перед `return` у блоці `existingChild` відновлюємо G зі збережених змінних батька; в `_refreshChildAnchors` перед `_computeAnchorOnCanvas` синхронізуємо `anchorDef.thickness` з `hostLineData._elementThickness`. Змінено: `shape-transfer.js`. |
| 2026-05-24 16:00 | v0.2.40 | Виправлено зсув усіх вкладених фігур при зміні товщини вікна будь-якого рівня. Причина: `_refreshChildAnchors` оновлював тільки прямих нащадків item, але не піднімався вгору по дереву — фігури що прив'язані до вікон у дочірніх кімнатах не перераховувались. Додатково SVG-групи що виявились відірваними від DOM поверталися назад в mainSvg. Додано: хелпер `_findParentInHierarchy`; після `_refreshChildAnchors(item)` рекурсивний підйом по предках через `propagateUp`; в `_refreshChildAnchors` — re-append відірваних груп та обробка дітей без `_anchorDef`. Змінено: `shape-transfer.js`. |
| 2026-06-02 14:00 | v0.4.0 | Панель ієрархії розділена на дві: «Елементи» (верхня) і «Властивості» (нижня). Висота кожної регулюється перетягуванням розділювача (мишею та дотиком). Панель «Елементи» — список фігур і вкладених елементів. Панель «Властивості» — деталі вибраного елемента (тип, назва, площа, кількість ліній/точок, елементи на лініях, дочірні). Одиночний клік на елементі у панелі «Елементи» — виділяє: синій фон у списку, червоний пунктирний контур на канві, заповнює «Властивості». Подвійний клік на фігурі у панелі «Елементи» або на канві — відкриває редактор фігур. При кліку на канві в режимі «Вибір» — виділяє фігуру аналогічно. Змінено: `index.html`, `app.css`, `hierarchy.js`, `shape-transform.js`, `main.js`. |
 Кардинальна зміна функціоналу. 1) Видалено режим редагування елементів вікна у редакторі фігур — `openElementInShapeEditor`, `_redrawElementEditorCanvas`, `viewingElementMode`, `_addingElementLine`, `addElementEditorLine`, `_updateWindowCornerAnchorUI`, `insertWindowCornerAnchor` вимкнено/видалено. 2) Видалено модалку «Товщина вікна» (`elementThicknessModal`) з `index.html` і `main.js`. 3) Видалено блок прив'язки до кута вікна (`windowCornerAnchorBox`) з coord modal. 4) Видалено блок режиму елемента з `updateLinesList`. 5) Редактор фігур тепер призначений виключно для побудови складних фігур. 6) Додано новий файл `shape-transform.js` — переміщення та обертання фігур на головному полотні через чотири режими інструменту: «Рука» (pan, drag полотна), «Вибір» (select, клік по фігурі), «Рухати» (move, drag вибраної фігури), «Крутити» (rotate, обертання вибраної фігури навколо центру). 7) Кнопки режимів у тулбарі: Рука (H), Вибір (V), Рухати (M), Крутити (R) з підсвічуванням активного інструменту. 8) Клавіатурні скорочення H/V/M/R. 9) CSS-клас `.active-tool` для підсвічування активної кнопки. Змінено: `index.html`, `main.js`, `coord-modal.js`, `shape-editor.js`, `lines-panel.js`, `canvas-manager.js`, `hierarchy.js`, `app.css`. Новий файл: `shape-transform.js`. |
| 2026-06-02 14:30 | v0.4.1 | Виділення елемента на канві: замість пунктирного прямокутника лінії вибраної SVG-групи забарвлюються в червоний (#ef4444); при виборі іншого елемента або знятті виділення — колір ліній відновлюється. Збережено атрибути `data-selected` / `data-orig-stroke` для надійного відновлення. Прибрано `_refreshHighlight` з `moveSelected` / `rotateSelected`. Змінено: `hierarchy.js`, `shape-transform.js`. |
| 2026-06-02 15:00 | v0.4.2 | Виправлено незалежність виділення фігур: клік на батьківській кімнаті більше не виділяє лінії вкладених елементів. Причина: `_highlightSvgItem` робив `querySelectorAll` рекурсивно по всьому піддереві групи. Виправлення: обходимо тільки прямі дочірні вузли; дочірні групи з `data-hierarchy-id` пропускаємо. Змінено: `hierarchy.js`. |
| 2026-06-03 12:00 | v0.4.3 | Виправлено виділення фігур кліком на канві. `_onMouseDown` у `shape-transform.js` дозволяв вибір тільки в режимі `select` — тепер виділення працює в усіх режимах (pan, select, move, rotate). Змінено: `shape-transform.js`. |
| 2026-06-03 13:30 | v0.4.4 | Виправлено виділення фігур і елементів: клік по фігурі виділяє її в будь-якому режимі; `_highlightSvgItem` не заходить у дочірні ієрархічні групи — кімната і вікно виділяються незалежно. Змінено: `hierarchy.js`, `shape-transform.js`. |
| 2026-06-03 15:00 | v0.4.5 | Реалізовано незалежне виділення вікон (WI1) на канві і в панелі ієрархії. Причина попередніх проблем: WI1 малювались прямо в SVG-групу батьківської кімнати без власного `<g data-hierarchy-id>`. Виправлення: додано `_drawElementsIntoGroups` і `_drawWI1inGroup` у `shape-transfer.js` — кожен WI1 отримує власну `<g data-hierarchy-id>` і реєструється як дочірній елемент ієрархії з `type: "element"`. `_fillSvgGroup` і `_rebuildSvgGroup` приймають `parentHierarchyItem`. У `hierarchy.js`: прибрано `detailsWrap` (замінено дочірніми елементами ієрархії); елементи `type: "element"` — повноцінні пункти з фіолетовою іконкою; клік показує властивості (код, лінія, від–до). Змінено: `shape-transfer.js`, `hierarchy.js`, `shape-transform.js`. |
| 2026-06-03 16:00 | v0.4.6 | Виправлено вибір фігури на канві: раніше активними для кліку були тільки тонкі лінії контуру — треба було точно потрапити по лінії. Виправлення: у `_fillSvgGroup` першим елементом у SVG-групу додається прозорий `<polygon fill="transparent">` що покриває всю площу фігури — тепер клік у будь-якому місці всередині фігури виділяє її. Змінено: `shape-transfer.js`. |
| 2026-06-03 17:00 | v0.4.7 | При виділенні фігури на канві з'являються червоні кружечки з номерами точок по всіх кутах. При знятті виділення — зникають. Враховується зміщення групи (`_offsetX/_offsetY`, `_anchorOnCanvas`) та `transform translate`. Змінено: `hierarchy.js`. |
| 2026-06-03 18:30 | v0.4.8 | Панель «Властивості» переписана як інтерактивна VB-style таблиця. Властивості згруповані (Ідентифікація, Геометрія, Площа, Позиція, Відображення, Елементи на лініях, Дочірні). Типи контролів: `info` (тільки читання), `string` (текстове поле), `number` (числове поле), `bool` (чекбокс Так/Ні), `select` (випадаючий список). Редаговані властивості: Назва, № приміщення, Площа, Видимий, Сторона елемента, Від/До елемента. При зміні «Видимий» — SVG-група показується/ховається одразу. Заголовок панелі — синя смуга з типом і назвою об'єкта. Фокус у полях — підсвічування синьою рамкою. Змінено: `hierarchy.js`. |
| 2026-06-03 20:00 | v0.4.9 | 1) Кнопка «+» у заголовку панелі властивостей — відкриває inline-діалог для додавання довільної (текстової) властивості в розділ «Користувацькі»; кожна така властивість редагується і видаляється кнопкою «✕». 2) Властивість «Розміри ззовні» тепер справно перемальовує фігуру на канві (зберігає/відновлює G.dimensionsOutside навколо _rebuildSvgGroup). 3) Вікно у панелі «Елементи» тепер називається «Вікно 1-2» (назва елемента + номери вузлів лінії). 4) WI1 отримує прозорий полігон-підкладку у власній SVG-групі — вся площа вікна клікабельна. 5) Додано властивість «Товщина (м)» для елемента WI1: при зміні значення — `lineData._elementThickness` оновлюється і батьківська SVG-група перемальовується. 6) При зміні «Від», «До», «Сторона», «Товщина» елемента — зміни одразу відображаються на канві. Змінено: `hierarchy.js`, `shape-transfer.js`. |
| 2026-06-03 21:00 | v0.4.10 | Виправлено: 1) «Розміри ззовні/всередині» повернуто до схеми кімнати (`room`) — було відсутнє. 2) Ключ товщини вікна в схемі `element` виправлено з `elThickness` на `_thickness` — тепер збігається з обробниками `_propGet`/`_propSet` і товщина справно застосовується на канві. 3) Числові поля в панелі властивостей тепер типу `text` з нормалізацією: кома замінюється на крапку під час введення (`oninput`) — курсор залишається на місці. Змінено: `hierarchy.js`. |
| 2026-06-03 21:30 | v0.4.11 | Виправлено: товщина вікна тепер індивідуальна для кожного WI1. Причина проблеми: `lineData._elementThickness` — одне поле на всю лінію, і зміна будь-якого вікна перезаписувала товщину для всіх. Виправлення: товщина зберігається в `elItem.elThickness`; `_drawElementsIntoGroups` для кожного WI1 окремо читає `elItem.elThickness` (якщо є) або лінійний/загальний fallback. Видалено присвоєння `lineData._elementThickness` з `_syncElementToParentAndRedraw`. Змінено: `shape-transfer.js`, `hierarchy.js`. |
| 2026-06-03 22:00 | v0.4.12 | Виправлено: зміна «Від»/«До» вікна в панелі Властивості тепер відображається у вікні «Введіть координати». Причина: `lineData.code` (рядок для редактора координат) не оновлювався після зміни `lineData.elements`. Виправлення: додано `_rebuildLineCode` — перебудовує рядок `direction\nlineType\nval...\ncode` з актуального масиву `elements`; викликається в `_syncElementToParentAndRedraw` після оновлення значень. Змінено: `hierarchy.js`. |
| 2026-06-03 20:00 | v0.4.9 | Панель Властивості: (1) Кнопка `+` у заголовку — відкриває inline-форму додавання користувацької властивості (назва + значення); користувацькі властивості зберігаються у `item._customProps`, відображаються у секції «Користувацькі» з можливістю редагування і видалення. (2) Виправлено «Розміри ззовні» — зміна чекбоксу тепер перемальовує SVG-групу фігури з новим `G.dimensionsOutside`. (3) Вікно у панелі «Елементи» тепер відображається як «Вікно 1-2» (назва + номери точок лінії). (4) WI1 отримує прозорий hit-polygon — клік у будь-якому місці площі вікна виділяє його. (5) Властивості «Від (м)», «До (м)», «Сторона», «Товщина (м)» вікна при зміні синхронізуються у `lineData` батьківської фігури і перемальовують SVG одразу. (6) Додано властивість «Товщина (м)» для елементів. Змінено: `hierarchy.js`, `shape-transfer.js`. |
| 2026-06-04 15:00 | v0.4.13 | Панель Властивості (room/building): поле «Тип» змінено з `info` (тільки читання) на `select` — випадаючий список з варіантами Кімната/Будинок/Контур; зміна типу відразу оновлює ієрархію і перемальовує властивості. Додано поле «Площа редагована (м²)» (`customArea`) — числове поле для ручного введення площі. Додано перемикач «Використовувати редаговану» (`useCustomArea`) — чекбокс що визначає яку площу показувати в панелі ієрархії; редагована площа відображається помаранчевим кольором. Підтримка нового типу `contour` у `createHierarchyItemElement` (іконка `fa-draw-polygon`, сірий колір) і в `renderProperties` (emoji ⬡). Змінено: `hierarchy.js`. |
| 2026-06-04 17:00 | v0.4.14 | Підпис кімнати у вигляді дробу (номер/площа). Нова функція `buildRoomLabel` у `svg-primitives.js` — будує конструкцію: номер кімнати зверху, горизонтальна риска, площа знизу; підтримує два режими: `inline` (традиційний, всередині кімнати) і `leader` (виносний — з лінією-виноскою від центру кімнати до підпису). У `_fillSvgGroup` (`shape-transfer.js`) вибирається між `buildRoomLabel` (якщо є площа) і `buildRoomNumberText` (тільки номер); враховуються `showRoomLabel`, `roomLabelStyle`, `useCustomArea`/`customArea` з `parentHierarchyItem`. У Властивостях (`hierarchy.js`) для `room` і `building` додано два перемикачі: «Показати підпис» (`showRoomLabel`, чекбокс) і «Вид підпису» (`roomLabelStyle`, select: Традиційний/Виносний); при зміні — SVG-група перемальовується через новий хелпер `_redrawItemSvgGroup`. Змінено: `svg-primitives.js`, `shape-transfer.js`, `hierarchy.js`. |
| 2026-06-04 18:30 | v0.4.15 | Виправлено виносну ніжку: лінія-виноска тепер йде від центру кімнати до ЛІВОГО кінця горизонтальної риски (не до центру дробу). Додано drag-переміщення конструкції-дробу мишею та дотиком: нова функція `_attachRoomLabelDrag` у `svg-primitives.js` вішає обробники `mousedown/mousemove/mouseup` і `touchstart/touchmove/touchend`; при перетягуванні `_rebuild(dx,dy)` оновлює вміст `<g>` без перемальовки всієї фігури, `onMove(dx,dy)` зберігає нове зміщення в `phi.leaderDx/leaderDy`. У `shape-transfer.js` передається `onMove` callback що зберігає зміщення в `parentHierarchyItem`. Змінено: `svg-primitives.js`, `shape-transfer.js`. |
| 2026-06-05 20:00 | v0.4.16 | Переміщення та обертання фігури разом з дочірніми елементами. `shape-transform.js` переписано: `_moveTree` рухає тільки SVG-transform групи (накопичується translate), `_rotateTree` обертає навколо спільного центру мас; element-типи (вікна) не рухаються окремо — вони всередині батьківської групи і рухаються автоматично; `_offsetX/_offsetY` не змінюються під час drag. Змінено: `shape-transform.js`. |
| 2026-06-05 21:00 | v0.4.17 | Виправлено відривання точок кутів від фігури при переміщенні та обертанні. Причина: `_highlightSvgItem` рахував `cx = pt.x + offsetX + tx` — враховував тільки translate, ігнорував rotate; при переміщенні translate подвоювався (offsetX + tx одночасно). Виправлення в `hierarchy.js`: позиції точок тепер обчислюються через `getScreenCTM()` групи — `groupCTM = svgScreenCTM.inverse() × grpScreenCTM`; ця матриця автоматично враховує будь-який SVG transform (translate + rotate + zoom). Локальна точка `(pt.x + offsetX, pt.y + offsetY)` трансформується через groupCTM у SVG-координати. Змінено: `hierarchy.js`, `shape-transform.js`. |
| 2026-06-05 22:00 | v0.4.18 | Виправлено: курсор тепер утримує фіксовану позицію відносно фігури протягом усього переміщення. Раніше використовувалось інкрементальне delta (pt - prevPt), через що курсор міг "відповзати" від точки захоплення. Тепер при mousedown зберігається `_anchorDx/Dy = pt - translate_групи`; на кожному mousemove translate встановлюється абсолютно: `newTx = pt.x - anchorDx`, потім рахується delta для `_moveTree`. Змінено: `shape-transform.js`. |
| 2026-06-05 23:00 | v0.4.19 | Виправлено нестабільне переміщення після обертання. Причина: `_anchorDx = pt.x - tx` читав тільки translate компонент, ігноруючи rotate — після обертання фактична SVG-позиція групи вже не збігалась з `tx/ty`. Виправлення: і при `mousedown`, і при `mousemove` реальна SVG-позиція початку координат групи обчислюється через CTM матрицю: `m = svgCTM.inverse() × grpCTM`; `m.e, m.f` — це SVG xy точки (0,0) локальної системи групи з урахуванням усіх трансформацій (translate + rotate). `_anchorDx = pt.x - m.e`. Тепер після будь-якого кручення переміщення залишається стабільним. Змінено: `shape-transform.js`. |
| 2026-06-05 23:30 | v0.4.20 | Виправлено дрижання при переміщенні та стрибок фігури при другому обертанні. Причина дрижання: при кожному mousemove читався CTM → рахувався origin → delta → moveTree → змінювався transform → наступний mousemove читав вже оновлений CTM, але браузер міг не встигнути оновити матрицю. Виправлення: threshold `abs(dx) > 0.01`. Причина стрибка при другому rotate: `_rotateTree` щоразу рахував `localCx = svgCx - tx` — при другому rotate `svgCx` (з getBBox) вже інший бо фігура повернута, але tx той самий → неправильний локальний центр. Виправлення: `cx0/cy0` — ФІКСОВАНИЙ центр обертання в локальній системі після translate; записується один раз при першому rotate і більше не змінюється. При наступних rotate тільки `ra` накопичується. Формат transform: `translate(tx,ty) rotate(ra,cx0,cy0)` де `cx0/cy0` фіксовані. Змінено: `shape-transform.js`. |
| 2026-06-05 23:50 | v0.4.21 | Підпис кімнати (номер/площа) тепер не обертається разом з фігурою і залишається читабельним при будь-яких трансформаціях. Механізм: після кожного `_rotateTree` і `_moveTree` викликається `_applyRoomLabelCounterRotate(item)` яка виставляє `transform="rotate(-ra, lcx, lcy)"` на `[data-room-label]` де `lcx,lcy` — центр мас фігури в локальній системі групи, `ra` — поточний кут обертання. Для leader-режиму: ніжка завжди тягнеться від центру мас фігури — `buildRoomLabel` тепер зберігає поточний центр у замиканні і додає метод `g._updateCenter(newCx, newCy)` для оновлення без повного перемалювання; `_applyRoomLabelCounterRotate` викликає `_updateCenter` з актуальним центром. При переміщенні (без rotate) counter-rotate = 0 — знімається, центр оновлюється. Змінено: `svg-primitives.js`, `shape-transform.js`. |
| 2026-06-06 12:00 | v0.4.22 | Панель Властивості: додано два нових поля для `room` і `building` у секції «Відображення» — «Шрифт підпису (мм)» (`labelFontSize`) і «Шрифт розмірів (мм)» (`dimensionFontSize`). Значення вводяться в міліметрах паперу (масштаб 1:100), зберігаються в мм, конвертуються в SVG-px при відображенні (`FONT_MM_TO_PX = 5`). Стандарт ЕСКД 3.5 мм = 17.5 px — значення за замовчуванням. При зміні — SVG-група перемальовується одразу. Змінено: `constants.js`, `hierarchy.js`, `shape-transfer.js`, `svg-primitives.js`. |
| 2026-06-06 12:30 | v0.4.23 | Відстань від лінії до підпису розміру автоматична: `offset = fontSize/2 + 2` (px) — при збільшенні шрифту текст не налазить на лінію. Змінено: `shape-transfer.js`. |
| 2026-06-06 15:00 | v0.4.25 | Виправлено три проблеми. 1) Відновлено виділення вікна (WI1) кліком на канві: `_findItemByTarget` більше не підміняє елемент його батьківською кімнатою — тепер повертається той елемент ієрархії, чия `<g data-hierarchy-id>` найближча до клікнутого SVG-вузла. 2) Заголовки закритих панелей акордеону мають синє тло (`#dbeafe`) з синім текстом; заголовок відкритої панелі — темно-синій (`#2563eb`) з білим текстом; hover-ефекти збережені. 3) Зазор між заголовками акордеону скорочено до 2 px через `gap: 2px` на `.hierarchy-panel`. Змінено: `shape-transform.js`, `app.css`. |
| 2026-06-06 16:00 | v0.4.26 | Виправлено акордеон бічної панелі та супутні проблеми. 1) Прибрано текстові заглушки «Немає елементів» і «Оберіть елемент» — порожні панелі тепер просто порожні. 2) CSS акордеону виправлено (попередній запис був зіпсований escaped-рядком): закриті заголовки — блакитні (#bfdbfe), відкритий — темно-синій (#2563eb) з білим текстом; пробіл між заголовками 2px через gap. 3) Відновлено виділення вікна (WI1) кліком на канві:  більше не підміняє елемент батьківською кімнатою. 4) При виборі елемента в ієрархії автоматично відкривається «Властивості». Змінено: , , . |
| 2026-06-07 17:00 | v0.4.31 | Спрощено swap у `_applyDiagonalConstraint` (`shape-editor.js`): якщо перше введене число більше другого — точки міняються місцями (`4 2 5` → обробляється як `2 4 5`). Попередній підхід через індекси `shapePoints` давав хибний результат після першої діагоналі. Змінено: `shape-editor.js`. |
| 2026-06-07 20:00 | v0.5.0 | Конструктиви: новий файл `constructs.js`. Панель «Конструктиви» — полоска перекриття 0.20 м, drag-and-drop на лінію фігури, автозахоплення (підсвічування), автоматичне розміщення між кутовими точками. Подвійний клік — видалити. Змінено: `constructs.js` (новий), `index.html`, `main.js`. |
| 2026-06-07 21:00 | v0.5.1 | Конструктиви: нова полоска не перекриває існуючі — знаходить вільний проміжок між перетинами ребер існуючих полосок з цільовою лінією. Змінено: `constructs.js`. |
| 2026-06-07 22:00 | v0.5.2 | Інтеграція конструктивів з ієрархією та властивостями. 1) Клік по елементу в панелі «Елементи» більше не перемикає автоматично на панель «Властивості» — прибрано `toggleAccordion` з `selectHierarchyItem`. 2) Полоска після розміщення реєструється в `G.hierarchyData` як елемент типу `construct`, з'являється в дереві «Елементи» з іконкою `fa-grip-lines`. 3) Клік по полоску на канві виділяє її в дереві. 4) Панель «Властивості» для `construct` показує: Назва, Товщина (м), «Початок від кінця B» (перемикач), «Довжина від початку (м)»; при зміні товщини або довжини полоска перемальовується. 5) Нова функція `_redrawConstructItem` у `constructs.js`. Змінено: `hierarchy.js`, `constructs.js`. |
| 2026-06-07 16:30 | v0.4.30 | Виправлено `_applyDiagonalConstraint` (`shape-editor.js`): при будь-якому порядку введення точок тепер завжди рухається та точка яка є ПІЗНІШОЮ в ланцюгу (більший індекс у `G.shapePoints`), а рання залишається фіксованою. Попередній swap-підхід давав геометрично невірний вибір `pFrom` (початок лінії до ранньої точки) і `p1` (пізня точка), через що `d=|pFrom→p1|` ставало занадто великим і кола не перетинались (помилка). Тепер: перед пошуком `lineIdx` індекси обох точок порівнюються і за потреби `pt1/pt2` міняються місцями щоб `pt2` завжди була пізнішою. Змінено: `shape-editor.js`. |
| 2026-06-07 15:00 | v0.4.29 | Два виправлення. (1) `shape-editor.js` — `_applyDiagonalConstraint`: при зворотному порядку введення (`4 2 5`) де між pt2(=2) і pt1(=4) є проміжні точки, підланцюг 2→3→4 тепер повертається як жорстке тіло (rigid-body rotation навколо нової точки 2) так щоб точка 4 залишалась на місці. Стара логіка тягнула лінії паралельно — точка 4 їхала кудись і фігура «прокручувалась». Зберігається стара позиція pt2 до оновлення для обчислення кута повороту. (2) `canvas-manager.js` — збереження і відкриття файлу тепер включає `hierarchyData`: при `saveCanvas` дані ієрархії (без DOM-посилань) серіалізуються у `<metadata data-bti>` всередині SVG; при `loadSvgFromContent` вони відновлюються, `svgGroup` кожного елемента прив'язується до відповідної `<g data-hierarchy-id>` у DOM, ієрархія відображається і фігури виділяються. |
| 2026-06-06 14:00 | v0.4.24 | Бічна панель переведена на акордеон з трьох секцій: «Елементи», «Властивості», «Конструктиви». Клік по заголовку секції відкриває її і закриває всі інші; одна секція завжди відкрита. Видалено розділювач між панелями та всю логіку його перетягування. При виборі елемента в ієрархії автоматично відкривається секція «Властивості». Додано порожню секцію «Конструктиви» (функціонал буде визначено пізніше). Змінено: `index.html`, `app.css`, `main.js`, `hierarchy.js`. |
| 2026-06-09 10:00 | v0.5.3 | Чотири нових можливості: 1) **Виділення полоски на канві** — клік на полоску підсвічує її червоним (`stroke:#ef4444`, `stroke-width:2.5`); нова функція `_highlightConstructItem` у `constructs.js`; `_highlightSvgItem` у `hierarchy.js` тепер знімає виділення з полосок і делегує `construct`-тип на `_highlightConstructItem`. 2) **Перемикач «Зсередини»** у властивостях полоски — нова bool-властивість `constructSideInward` (false=ззовні, true=зсередини); `_redrawConstructItem` враховує знак нормалі залежно від значення; `PROP_SCHEMA.construct` доповнено. 3) **Видалення виділеного елемента** — нова функція `deleteSelectedHierarchyItem` у `hierarchy.js`; кнопка «🗑» у заголовку панелі «Властивості»; кнопка «Видалити» в головному тулбарі; клавіша `Delete`/`Backspace` (у `main.js`). 4) **Прив'язка кімнати до конструктиву** — при відпусканні миші/дотику в режимі «Рухати»: якщо будь-яка точка кімнати ближча ніж 20 SVG-px до кута полоски — кімната миттєво зміщується щоб точки збіглись; нові функції `_getConstructSnapPoints` у `constructs.js` і `_snapRoomToConstruct` у `shape-transform.js`. Змінено: `constructs.js`, `hierarchy.js`, `shape-transform.js`, `index.html`, `main.js`. |
| 2026-06-09 11:00 | v0.5.4 | Конструктив «Вікно WI1»: drag-and-drop з панелі «Конструктиви» на лінію фігури. Сторона прилипання визначається автоматично — знак cross-product вектора лінії та вектора до курсора (`cross >= 0 → side=1`, `< 0 → side=-1`). При відпусканні вікно WI1 (0.90 × 0.20 м за замовчуванням) розміщується по центру точки кидання; реєструється як дочірній `element`/`elCode='WI1'` батьківської фігури; вставляється тріплет `{elStart, elEnd, 'WI1'}` у `lineData.elements`. Клік на вікні — виділення в панелі «Елементи» та «Властивості» з полями «Від (м)», «До (м)», «Сторона», «Товщина (м)». При зміні цих полів: `_syncElementToParentAndRedraw` визначає drag-вікно по ключу `wi_drag_*` і викликає `_redrawWindowElement` (перемальовує тільки власну `<g>`, без перебудови всієї групи батька). Нові функції: `_makeWindowStrip`, `_startWindowDrag`, `_detectWindowSnapLine`, `_finishWindowDrop`, `_placeWindowOnLine`, `_drawConstructWI1`, `_redrawWindowElement` — усі в `constructs.js`. Змінено: `constructs.js`, `hierarchy.js`. |
